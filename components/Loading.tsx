import { LoadingStyles } from "./LoadingStyles"

export const Loading = () => {
    return (
        <div>
            <LoadingStyles>
                <img src="loading.gif"/>
            </LoadingStyles>
        </div>
    )
}